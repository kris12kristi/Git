@model IEnumerable<Laba8.Models.Kafedra>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Kafedra_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Kafedra_zav)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Count_Doctor_Science)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Instytyts_id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Kafedra_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Kafedra_zav)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Count_Doctor_Science)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Instytyts_id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Kafedra_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Kafedra_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Kafedra_id })
        </td>
    </tr>
}

</table>
