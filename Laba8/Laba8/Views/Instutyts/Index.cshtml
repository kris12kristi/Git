@model IEnumerable<Laba8.Models.Instutyt>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Instytyts_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Director)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Instytyts_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Director)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Instytyts_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Instytyts_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Instytyts_id })
        </td>
    </tr>
}

</table>
